# ==============================================
# OPENGL SETTINGS
# ==============================================

opengl {
    # Reduces flickering on NVIDIA GPUs at the cost of possible frame drops
    # On non-NVIDIA systems, this setting is ignored
    # Recommended: true for NVIDIA users experiencing flickering
    # Recommended: false for maximum performance or non-NVIDIA systems
    nvidia_anti_flicker = false   
}
