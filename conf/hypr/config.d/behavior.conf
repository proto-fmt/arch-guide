# ==============================================
# WINDOW & FOCUS BEHAVIOR SETTINGS
# ==============================================

binds {
    # === MOUSE & DRAGGING BEHAVIOR ===
    # pass_mouse_when_bound = false     # Pass mouse events after keybinds
    # scroll_event_delay = 300          # Delay between scroll events for binds
    # drag_threshold = 0                # Pixel threshold for window dragging
    
    # === WORKSPACE NAVIGATION ===
    # workspace_back_and_forth = false  # Switch to previous workspace on same
    workspace_center_on = 1             # Cursor position on workspace switch
    # allow_workspace_cycles = false   
    # hide_special_on_workspace_change = false # Auto-hide special workspace
    
    # === FOCUS & MOVEMENT BEHAVIOR ===
    # focus_preferred_method = 0        # Focus method: 0=history, 1=geometry
    # movefocus_cycles_fullscreen = false  # Cycle fullscreen windows
    # movefocus_cycles_groupfirst = false # Cycle grouped windows first
    # window_direction_monitor_fallback = true # Move between monitors at edges
    
    # === GROUP BEHAVIOR ===
    # ignore_group_lock = false         # Ignore group locks for movements
    
    # === SECURITY & COMPATIBILITY ===
    # disable_keybind_grabbing = false  # Allow apps to disable keybinds
    # allow_pin_fullscreen = false      # Allow fullscreen for pinned windows
}
