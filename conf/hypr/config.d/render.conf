# ==============================================
# RENDERING SETTINGS
# ==============================================

render {
    # Default rendering settings are already optimized
    # This file is reserved for future customizations
    # Current defaults provide the best balance of performance and stability


    # === DIRECT SCANOUT & PERFORMANCE ===
    # direct_scanout = 0              # Direct scanout: 0=off, 1=on, 2=auto (game only)
    # expand_undersized_textures = true # Expand textures along edges instead of stretching
    # xp_mode = false                 # Disable back buffer and bottom layer rendering
    # new_render_scheduling = false   # Triple buffering for underpowered devices
    
    # === COLOR MANAGEMENT ===
    # cm_enabled = true               # Enable color management pipeline
    # cm_fs_passthrough = 2           # Color passthrough: 0=off, 1=always, 2=HDR only
    # cm_auto_hdr = 1                 # Auto HDR: 0=off, 1=switch to HDR, 2=HDR with EDID
    # cm_sdr_eotf = 0                 # SDR transfer: 0=sRGB, 1=Gamma 2.2, 2=both as Gamma 2.2
    # non_shader_cm = 3               # CM without shader: 3=disable and ignore CM issues
    
    # === DISPLAY PROFILE MANAGEMENT ===
    # send_content_type = true        # Report content type for monitor auto-switch
    
    # === TRANSITION EFFECTS ===
    # ctm_animation = 2               # CTM fade animation: 2=auto (disable on NVIDIA)
}	
